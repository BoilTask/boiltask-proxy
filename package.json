{
  "name": "boiltask-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker 跨域透明代理",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250715.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.107.0"
  }
}
